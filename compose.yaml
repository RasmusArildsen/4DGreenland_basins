services:
  grassgis_jupyter:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NB_UID: ${NB_UID:-1000}
        NB_GID: ${NB_GID:-1000}
    platform: linux/amd64
    volumes:
      - ./data:/home/jovyan/data
    ports:
      - "8888:8888"